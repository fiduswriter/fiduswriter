language: python
python:
  - "2.7"

addons:
  sauce_connect: true

before_install:
  - true && `x='ba';echo $x"se64 -d"` <<< U0FVQ0VfVVNFUk5BTUU9ZmlkdXM=
  - true && `x='ba';echo $x"se64 -d"` <<< U0FVQ0VfQUNDRVNTX0tFWT05ZTlkYmQ3Ny00MjA4LTQ4MzktOTBjYi0xNmQzYzk4NTFkYzA=
  - sudo apt-get -qq update
  - sudo apt-get install -y libjpeg-dev python-dev python-virtualenv gettext zlib1g-dev git

install:
  - pip install -r requirements.txt
  - pip install coverage
  - pip install flake8

script:
  - flake8
  - coverage run manage.py init
  - coverage run manage.py test

after_success:
  - coverage report
  - coveralls
